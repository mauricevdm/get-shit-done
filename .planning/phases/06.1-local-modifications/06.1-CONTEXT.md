# Phase 06.1 Context: Local Modifications Integration

**Created:** 2026-02-24
**Phase Goal:** Add missing finalize-phase command file to enable /gsd:finalize-phase

## Scope Clarification

This fork is a **globally-installed tool** (`~/.claude/get-shit-done/`). Users install it once and use it across multiple projects. Project-specific customizations go in `.claude/` within each project.

**What exists:**
- ✅ `finalize-phase.md` workflow (get-shit-done/workflows/) — comprehensive, 630 lines
- ✅ `execute-phase.md` with worktree auto-creation
- ✅ All path references correctly point to `~/.claude/`

**What's missing:**
- ❌ `finalize-phase.md` command file in `get-shit-done/commands/gsd/`

Without the command file, `/gsd:finalize-phase` cannot be invoked.

## Changes to Implement

### 1. Create finalize-phase Command File

**File to create:** `get-shit-done/commands/gsd/finalize-phase.md`
**Source reference:** ~/Projects/OpenClaw/gsd/commands/gsd/finalize-phase.md
**Purpose:** Command wrapper that triggers the finalize-phase workflow

The command file is a small YAML frontmatter + markdown file that:
- Defines the command name and description
- Specifies allowed tools
- References the workflow file

## Out of Scope

- Path updates (paths are correct as `~/.claude/`)
- Workflow modifications (workflow is already present and complete)
- Auto-worktree creation (already present in execute-phase.md)

## Deferred Ideas

- None

---
*Context created: 2026-02-24*
*Updated: 2026-02-24 — Corrected scope after user clarification*
