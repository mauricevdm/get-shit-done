---
phase: 06.1-local-modifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/commands/gsd/finalize-phase.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can invoke /gsd:finalize-phase command"
    - "Command triggers finalize-phase workflow"
  artifacts:
    - path: "get-shit-done/commands/gsd/finalize-phase.md"
      provides: "Command file for /gsd:finalize-phase"
      contains: "gsd:finalize-phase"
  key_links:
    - from: "finalize-phase.md command"
      to: "finalize-phase.md workflow"
      via: "@gsd/get-shit-done/workflows/finalize-phase.md"
---

<objective>
Add the missing finalize-phase command file to enable /gsd:finalize-phase

Purpose: Allow users to finalize completed phases by merging to main and cleaning up worktrees.

Output: Command file at get-shit-done/commands/gsd/finalize-phase.md
</objective>

<execution_context>
@gsd/get-shit-done/workflows/execute-plan.md
@gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-local-modifications/06.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create finalize-phase command file</name>
  <files>get-shit-done/commands/gsd/finalize-phase.md</files>
  <action>
Create the finalize-phase command file with the following content:

```markdown
---
name: gsd:finalize-phase
description: Finalize a phase: verify gates (UAT, tests, verification), merge to main, cleanup worktree
argument-hint: "<phase-number>"
allowed-tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Bash
  - Task
  - AskUserQuestion
---
<objective>
Finalize a completed phase by verifying all quality gates, merging the phase branch to main, and cleaning up the git worktree.

**Quality Gates:**
1. UAT passed (or verification-only for infra phases)
2. Tests pass (if test suite exists)
3. Verification passed (no gaps)

**Actions:**
1. Merge phase branch to main (no-ff)
2. Remove worktree directory
3. Release phase lock
4. Update STATE.md
</objective>

<execution_context>
@gsd/get-shit-done/workflows/finalize-phase.md
@gsd/get-shit-done/references/ui-brand.md
</execution_context>

<context>
Phase: $ARGUMENTS

@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<process>
Execute the finalize-phase workflow from @gsd/get-shit-done/workflows/finalize-phase.md end-to-end.

**Gate Order:**
1. Check UAT status → must be "passed"
2. Check Verification status → must be "passed"
3. Run tests → must pass (or not exist)
4. Merge to main → must succeed
5. Cleanup worktree → remove and release lock
6. Update state → record finalization

Stop at first gate failure and report what needs to be fixed.
</process>
```
  </action>
  <verify>
Verify the file exists and has correct structure:
```bash
ls -la get-shit-done/commands/gsd/finalize-phase.md
grep "name: gsd:finalize-phase" get-shit-done/commands/gsd/finalize-phase.md
```
Should show file exists with correct command name.
  </verify>
  <done>finalize-phase.md command file exists and references the workflow</done>
</task>

</tasks>

<verification>
1. File exists at get-shit-done/commands/gsd/finalize-phase.md
2. File contains correct YAML frontmatter with name: gsd:finalize-phase
3. File references @gsd/get-shit-done/workflows/finalize-phase.md
</verification>

<success_criteria>
- finalize-phase.md command file created
- Command references the existing workflow
- No syntax errors in YAML frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-local-modifications/06.1-01-SUMMARY.md`
</output>
